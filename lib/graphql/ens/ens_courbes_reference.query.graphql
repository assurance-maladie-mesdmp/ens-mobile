query get_courbes_reference($params: MeasureGrowthMetricsParams!) {
    measureGrowthMetrics(params: $params) {
        categories {
            name
            data {
                date
                measure
            }
        }
    }
}