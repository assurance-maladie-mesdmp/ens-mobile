fragment lastMesure on MeasureModel {
    id
    code
    effectiveDate
    metrics {
        unit
    }
    label
    formattedMetricValue
    permissions {
        add
        history
        update
        delete
    }
}
