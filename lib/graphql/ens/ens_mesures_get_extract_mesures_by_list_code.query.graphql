# import './ens_mesure_extract_mesure.fragment.graphql'

query get_extract_mesures_by_list_code($measurePageableInputs: [MeasurePageableInput!]!) {
    measuresByCodeList(measurePageableInputs: $measurePageableInputs) {
        ...extractMesure
    }
}
